= link_to 'Back to products list', category_products_path, :class => "btn btn-primary"

.row.product.thumbnail
  .col-sm-12
    .product-header
      %p.product-price.pull-right.lead
        %span.glyphicon.glyphicon-usd{aria: {hidden: true}}
        = product.price
      %h3.product-title
        = product.title
        - if current_user == product.user
          = link_to 'Edit', edit_category_product_path(category.id, product.id),
            class: 'btn btn-warning'
          = link_to 'Destroy',
          category_product_path(product.category, product),
          method: :delete,
          data: { confirm: 'Are you sure?' },
          class: 'btn btn-danger'
    %p
      %strong Category:
      = product.category.name
    %p.product-desc
      = product.description
    .ratings
      %p.product-reviews.pull-right
        %strong Reviews:
        = product.product_reviews_count
      %p.product-rating
        %strong Average rating:
        = product.average_rating



%h3 Reviews
.well.reviews
  = render reviews
  %h4 Your review
  = render 'reviews/form'

